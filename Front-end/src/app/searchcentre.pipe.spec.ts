import { SearchcentrePipe } from './searchcentre.pipe';

describe('SearchcentrePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcentrePipe();
    expect(pipe).toBeTruthy();
  });
});
